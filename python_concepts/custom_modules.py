# CUSTOM MODULES

# We can import custom modules by importing the file name directly in the script itself.

import example_module

example_module.greeting("Tom")

# Because the example module has the function "Hello + name" 
# this prints "Hello Tom"
